from interface import Interface
# from nn import NN

# network = NN('124M')

# network.train_model(file_name='datasets/the_iliad/the_iliad.txt', run_name='the_iliad', steps=12000,
                    # sample_length=100)

# Creates an instance of a browser using Eel.
gui = Interface()

# Create the window
gui.create_window()
